AI toggle — ViewState.aiEnabled boolean + toolbar button. When on, creation commands call an AI use-case instead of templates. Not started.
✅weavePlan --parent — optional parent design link at creation time (Flow 2 from happy-path chat). One small app/ change.
Phase 4 — Weave→Thread subdirs. Agreed to defer.