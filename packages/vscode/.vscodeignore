src/**
node_modules/**
tsconfig.json
tsconfig.tsbuildinfo
esbuild.js
package-old-loom.json
*.vsix
.vscode/**
.claude/**

# Monorepo siblings — already bundled by esbuild into dist/extension.js
../**

# Dist: only the bundled outputs are needed — the extension itself and the
# Loom MCP server (dist/loom-mcp.js) the extension spawns on Electron-as-Node.
dist/**
!dist/extension.js
!dist/loom-mcp.js
