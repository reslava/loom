---
type: plan
id: pl_01KXJK8PGC40JMRPN5P2PMHJJ1
title: Encode the light-README-entry-point philosophy in the doc-sync contract
status: done
created: 2026-07-15
updated: 2026-07-15
version: 1
design_version: 1
tags: []
parent_id: null
requires_load: []
target_version: 0.1.0
steps:
  - id: rewrote-the-doc-sync-contract-s
    order: 1
    status: done
    description: "Rewrote the doc-sync contract's README/docs sweep paragraph in CLAUDE.md: named the root README.md alongside the two package READMEs, declared the three READMEs deliberately light entry points (hero + quick-start + Learn-more doc-map), pointed structural-change depth fixes at docs/*.md, and added the rule to never re-inline exhaustive detail into a README to fix drift — update the linked doc instead. Repo-specific section (no rule: marker, no invariant token touched), so CLAUDE.md ⇄ LOOM_CLAUDE_MD parity is preserved and the template is untouched."
    files_touched: []
    blocked_by: []
    satisfies: []
---
# Encode the light-README-entry-point philosophy in the doc-sync contract

## Goal

Rewrote the doc-sync contract's README/docs sweep paragraph in CLAUDE.md: named the root README.md alongside the two package READMEs, declared the three READMEs deliberately light entry points (hero + quick-start + Learn-more doc-map), pointed structural-change depth fixes at docs/*.md, and added the rule to never re-inline exhaustive detail into a README to fix drift — update the linked doc instead. Repo-specific section (no rule: marker, no invariant token touched), so CLAUDE.md ⇄ LOOM_CLAUDE_MD parity is preserved and the template is untouched.

---

## Steps

| Done | # | Step | Files touched | Blocked by | Satisfies |
|---|---|---|---|---|---|
| ✅ | 1 | Rewrote the doc-sync contract's README/docs sweep paragraph in CLAUDE.md: named the root README.md alongside the two package READMEs, declared the three READMEs deliberately light entry points (hero + quick-start + Learn-more doc-map), pointed structural-change depth fixes at docs/*.md, and added the rule to never re-inline exhaustive detail into a README to fix drift — update the linked doc instead. Repo-specific section (no rule: marker, no invariant token touched), so CLAUDE.md ⇄ LOOM_CLAUDE_MD parity is preserved and the template is untouched. | — | — | — |
---

### Legend

| Symbol | Meaning |
|--------|---------|
| ✅ | Done |
| 🔄 | In Progress |
| 🔳 | Pending |
| ❌ | Cancelled |
