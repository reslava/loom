---
type: done
id: pl_01KXE1ZMFPJ9R2JMZCFWB5M3K5-done
title: Done — Split pointed context into load (heavy-once) vs read/reply (doc-only)
status: done
created: 2026-07-13
version: 8
tags: []
parent_id: pl_01KXE1ZMFPJ9R2JMZCFWB5M3K5
requires_load: []
---
# Done — Split pointed context into load (heavy-once) vs read/reply (doc-only)

## Step 1 — Add a scope option to assembleContext ('full' default | 'doc'): in 'doc' mode emit only the target doc — skip global/weave ctx, always-refs, the parent chain, user-includes, and requires_load — while still populating the weaveSlug/threadUlid header and honoring the alreadyLoaded ledger (a read of a doc already held returns an empty delta). Parse scope in handleContextResource and pass it through; must compose with ?mode=chat. Keep the assembler pure.

Added a `scope: BundleScope = 'full'` parameter to `assembleContext` (packages/app/src/context/assembleContext.ts). Exported a new `BundleScope = 'full' | 'doc'` type with doc comment. In `'doc'` mode the entire surrounding-bundle population — global/weave ctx (2a/2b), always-refs (2d/3), the parent chain (4), user-includes (5), and requires_load (6) — is skipped by wrapping those blocks in `if (scope === 'full')`; only `add(targetEntry.doc, 'target', 'auto')` runs. The `weaveSlug`/`threadUlid` header and the `alreadyLoaded` ledger split are outside the guard, so a doc-scope bundle still carries the active-thread address and a read of an already-held doc returns an empty delta. Assembler stays pure. In packages/mcp/src/resources/context.ts, parsed `?scope=doc` (default 'full') and passed it as the new 6th arg; it composes with `?mode=chat` since scope only gates the surrounding bundle, not target resolution.

## Step 2 — Add tests/context-scope-doc.test.ts (dist-importing, tests/test-utils assert style; register a run_test line in scripts/test-all.sh): assert ?scope=doc returns exactly the target doc with no ctx/parent-chain/requires_load docs, that the bundle header still carries threadUlid, that ?mode=chat still resolves the chat target, and that declaring the target in ?loaded= yields an empty delta.

Added tests/context-scope-doc.test.ts (dist-importing, tests/test-utils `assert`, `run().catch(... process.exit(1))` style — matching context-assembler.test.ts). Fixture: global ctx + weave ctx + idea/design/plan + a requires_load ref + a target chat, with thread.manifest.id set so the header assertion has a real th_ ULID. Assertions: (1) full scope still bundles ctx + parent chain + requires_load (default behaviour preserved); (2) doc scope emits exactly the target — docs.length===1, scope==='target', and none of g-ctx/w-ctx/i1/d1/p1/rf-A leak in; (3) header still carries weaveSlug + threadUlid + targetId in doc scope; (4) doc scope on a non-chat target (read design) returns only the design; (5) doc scope + alreadyLoaded=[target@1] yields an empty delta with the target recorded in the manifest and the threadUlid header intact. Registered `run_test tests/context-scope-doc.test.ts` in scripts/test-all.sh next to context-assembler. Tested at the pure-assembler level (matching context-assembler.test.ts); the resource-layer ?scope=doc parse is a trivial passthrough covered by the mcp integration surface.

## Step 3 — Rewrite loom/refs/loom-slang-reference.md (via loom_update_doc) for the split: add `load {weave}/{thread}` (heavy, sets the AI-held active thread, maps to loom://context/thread/...); redefine `read` and `reply` as doc-only on the active thread via ?scope=doc. Document the active-thread convention (AI-held, server is stateless), bare-filename resolution within the active thread, full weave/thread/doc only at session start or thread switch, first-pointed-action-implicitly-loads (Q2a), read never auto-loads (Q1), the read-vs-reply cross-thread asymmetry (read {full path} = pure fetch, no switch; reply {full path} to a non-active thread refused + prompt to load, Q3), and the `load X, reply Y` chain. Preserve the earned-slang framing and stop-rule alignment.

Rewrote loom/refs/loom-slang-reference.md body via loom_update_doc (now v3). Added a new "The active thread — load once, read/reply cheap" section covering: load {weave}/{thread} → loom://context/thread/... as the only verb that sets/switches the AI-held active thread (server is stateless), emitting `🧵 Active: {weave}/{thread}`; bare filenames resolve inside the active thread; first pointed action of a session implicitly loads (Q2a); read never auto-loads/switches after that and a cross-thread read is a pure doc-only peek (Q1); reply refuses to cross threads — a full path naming a non-active thread is refused with a prompt to load first (Q3), with the `load otherWeave/otherThread, reply chat-001` switch chain. Reworked the vocabulary table (added the `load` row; redefined `read`/`reply` as doc-only via ?scope=doc with active-thread + full-path forms) and the explicit chains (added the `load` chain, rewrote the read/reply chains). Added a "No unload word" entry under What is NOT slang (can't evict context; the split's win is not-reloading, not reclaiming). Preserved the earned-slang framing, do/quick families, code-vs-write-quick, docs-done recipe, and stop-rule alignment. Also updated the frontmatter `description` to include `load` (direct edit — no MCP tool sets description; loom/refs/* is gate-excluded).

## Step 4 — Update the Loom slang section of docs/WAYS-TO-USE-LOOM.md to summarize the load/read/reply split and link loom-slang-reference.md, framed for the Power terminal / Pure agent ways where the words remove real friction. Verify the availability clause still holds (no advertised way relies on a capability the slang can't reach).

Updated docs/WAYS-TO-USE-LOOM.md slang section: added a `load {weave}/{thread}` row (heavy-once, sets active thread, shows 🧵 Active), redefined `read` as doc-only (`?scope=doc`, active-thread + full-path forms) and kept `reply`; added a "`load` once, then `read`/`reply` cheap" explainer paragraph (bare filenames resolve in the active thread, full path only at session start/switch, cross-thread read = harmless peek, reply stays in the active thread with the `load weaveA/threadA, reply chat-001` switch chain). Also aligned the "Borrow context from another thread" pattern to a doc-only pointed read (`?scope=doc`) that doesn't switch the active thread.

Availability-clause check → found the CLI `loom context <doc>` twin lacked a scope flag, so mirrored `?scope=doc` as `--scope <scope>` (tri-surface parity): packages/cli/src/commands/context.ts now builds the query via URLSearchParams for mode+scope, and packages/cli/src/index.ts registers `--scope <scope>` (full default | doc). This keeps the extension-free Pure-agent way fully runnable and the CLI ⇄ MCP surfaces in step (files beyond the listed docs: packages/cli/src/commands/context.ts, packages/cli/src/index.ts).

## Step 5 — Update the shared rule:loom-slang block in CLAUDE.md and the LOOM_CLAUDE_MD template (packages/app/src/installWorkspace.ts), keeping the matching <!-- rule:loom-slang --> markers so tests/claude-md-sync.test.ts stays green, with voice tailored per surface. Also align CLAUDE.md's own sections that describe pointed reads to the new semantics: 'Human pointer -> slug-path context resource', the 'Chat-reply context injection' block, and session-start step 5 — so load/read/reply are consistent everywhere. These are the load-once / read-doc-only / reply-cross-thread-refused rules.

Updated the shared rule:loom-slang block in both CLAUDE.md and the LOOM_CLAUDE_MD template (packages/app/src/installWorkspace.ts), keeping the <!-- rule:loom-slang --> markers: added a `load {weave}/{thread}` bullet (full thread bundle, sets the AI-held active thread, emits 🧵 Active, only verb that switches, first-pointed-action implicitly loads), redefined `read` as doc-only (`?scope=doc`, active-thread bare form + full path, never auto-loads/switches after first touch, cross-thread read = pure peek, still implies reply for a pending chat), and extended `reply` (bare {chatSlug} in active thread; full path honored only for the active thread, else refused → load first). Aligned three more shared sections in BOTH surfaces to the split: rule:human-pointer-context (added the doc-only `?scope=doc` form for the active thread), rule:context-injection (the first-touch full-bundle read IS the `load` slang → sets active thread, later read/reply doc-only; cross-thread reply refused until load), and session-start step (the deep pointed-thread load IS `load`; later reads doc-only). No markers added/removed and the verbatim invariants (MCP prefixes, loom_* write-path names, stop-rule phrases) are untouched, so tests/claude-md-sync.test.ts parity holds (verified in step 8).

## Step 6 — Update the extension launch prompts (packages/vscode/src/commands/*.ts) so extension-launched Claude agents get the same load/read/reply slang and active-thread semantics. Mirror the reference wording; keep it terse.

No code change needed — the step's goal (extension-launched agents get the load/read/reply slang + active-thread semantics) is already met, and editing a launch prompt would be noise. Reasoning, verified by reading the launch prompts: (1) The only commands with context/reply wording are chatReply.ts and req.ts; neither teaches read/reply slang — they are one-shot task prompts. (2) chatReply.ts injects the FULL thread bundle via `loom://context/${chatId}?mode=chat` and tells the agent "do NOT grep or re-read these" — this is exactly the correct first-touch `load` (a freshly launched agent holds nothing, so a doc-only `?scope=doc` read would be wrong here). (3) Launched agents read the installed `.loom/CLAUDE.md`, which is the LOOM_CLAUDE_MD template updated in step 5, so they inherit the `load`/`read`/`reply` split, active-thread convention, and cross-thread reply refusal. No launch prompt encodes now-stale semantics. Confirmed the earlier flag (that step 6 might touch several files) resolves to zero edits after checking which prompts carry slang text: none do.

## Step 7 — Document the new ?scope=doc query param on the slug-path context resource in loom/refs/mcp-reference.md (doc-sync contract: the MCP resource surface map), noting it returns only the target doc and still carries the threadUlid header.

Documented `?scope=doc` in loom/refs/mcp-reference.md §1 Resource templates: extended the `loom://context/...` row's query-param list to include `?scope={full|doc}`, and added a dedicated doc-only row (`loom://context/{weaveSlug}/{threadSlug}/{docSlug}?scope=doc`) noting it returns only the target doc — no ctx/parent-chain/requires_load — while the header still carries weave_slug + thread_ulid and the ?loaded= ledger still dedupes, and that it backs the read/reply slang path (a doc in an already-loaded thread costs just that doc). Direct edit (loom/refs/* is gate-excluded). Honors the doc-sync contract's MCP-surface row (mcp-reference.md is the human-readable resource map).

## Step 8 — Run ./scripts/build-all.sh then ./scripts/test-all.sh; confirm the new context-scope-doc test and tests/claude-md-sync.test.ts pass, and spot-check ?scope=doc against the live rebuilt server (a fresh session, since build-all does not restart a running MCP). Fix any failures.

Ran ./scripts/build-all.sh (all packages clean, CLI relinked, extension bundled) then ./scripts/test-all.sh — full suite green, including the MCP integration subprocess suite (23 passed, 0 failed). Confirmed the two critical tests directly: tests/context-scope-doc.test.ts passes all 5 assertions (full-scope preserved; doc-scope emits only the target; header keeps weaveSlug+threadUlid; read-design doc-only; already-held target → empty delta), and tests/claude-md-sync.test.ts passes (17 shared rule ids match across both CLAUDE.md surfaces, 12 invariant tokens present) — so the load/read/reply rule edits kept parity. Live end-to-end spot-check via the rebuilt CLI (which spawns its own server from the new dist, sidestepping this session's stale in-process MCP): `loom context .../design` → docs=6 (full bundle); `loom context .../design --scope doc` → docs=1, only `### [target design]`, header still carries thread_ulid; `loom context .../chat-002 --scope doc --mode chat` → docs=1, mode=chat, `### [target chat]` — scope=doc composes with mode=chat. Note for a fresh session: this session's own loom MCP still runs pre-rebuild code, so a `read ...?scope=doc` issued through it would return the full bundle until the session/MCP restarts (expected, per the build-doesn't-restart-MCP gotcha).
