---
type: done
id: pl_01KSG7MW7ZZJ2R9D76EPEAX02G-done
title: Done — Context Pipeline — Phase 1
status: done
created: "2026-05-25T00:00:00.000Z"
version: 8
tags: []
parent_id: pl_01KSG7MW7ZZJ2R9D76EPEAX02G
requires_load: []
---
# Done — Context Pipeline — Phase 1

## Step 1 — Define core context types in packages/core: ContextBundle, BundledDoc, ExcludedDoc, plus the OperationMode, DocScope, EmitReason and ExcludeReason unions. Export from the core index. Types only, no logic. (Files: packages/core/src/entities/context.ts + index export.) Verify: tsc builds; types importable from app and mcp.

Added `packages/core/src/entities/context.ts` defining `OperationMode`, `DocScope`, `EmitReason`, `ExcludeReason`, `ContextOverrides`, `BundledDoc`, `ExcludedDoc`, `ContextBundle`. Exported all from `packages/core/src/index.ts`. Types only, no logic. Grounded against `BaseDoc` (`content` carries the body, so the assembler can stay pure) and `DocumentType` (reused for `BundledDoc.type`).

## Step 2 — Implement the pure assembler packages/app/src/context/assembleContext.ts with signature (targetId, mode, overrides, state) => ContextBundle. Phase-1 behaviour only: auto-load global/weave/thread ctx (all ctx treated as load:always), add the target's parent chain (idea/design/plan), resolve requires_load eagerly + transitively with a visited-set for cycle safety, emit a missing-target placeholder BundledDoc{missing:true} (+ excluded 'missing') for dangling ids, compute tokenEstimate = ceil(chars/4), flag stale via existing staleness helpers, and apply the deterministic ordering (global ctx -> weave ctx -> thread ctx -> refs -> parent chain -> target -> requires_load refs). Include a pure classifyScope(doc, state) helper. NO load_when, NO override logic beyond accepting an empty {include,exclude}, NO budget. Unit tests in tests/context-assembler.test.ts over a hand-built LoomState fixture: scope ordering, transitive + cyclic requires_load termination, missing-target placeholder, stale flagging.

Added `packages/app/src/context/assembleContext.ts` — pure `assembleContext(targetId, mode, overrides, state)`. Builds an id→doc catalog from `LoomState` in one traversal (the `LinkIndex` only maps id→path, so the pure fn needs its own in-memory map). Auto-loads global/weave/thread ctx by positional scope, adds the thread parent chain (idea/design/active-plan), resolves `requires_load` eagerly+transitively with a visited-set (cycle-safe), resolves slugs via `resolveId(state.index, …)`, emits a `missing:true` placeholder + `excluded:'missing'` for dangling ids, estimates tokens (`ceil(chars/4)`), and flags stale plans via `isPlanStale`. Exported `classifyScope` helper. Implemented exclude/include override application (cheap + correct) rather than a dead param — the Phase-3 work is the sidebar that *produces* overrides. Findings: (1) `getState`/`loadThread` do NOT load thread-root or `ctx/`-subdir ctx docs, so thread/weave ctx auto-load is wired but inert until `getState` is extended (defer to ctx-naming/global-ctx threads); today only the global `loom/ctx.md` exists, so the chat-reply fix works via global ctx + parent chain. (2) `loom/refs/*.md` load as a pseudo-weave 'refs', so vision/workflow resolve through `requires_load` slugs. Tests: `tests/context-assembler.test.ts` (registered in test-all.sh) — 9 assertions green.

## Step 3 — Implement bundle serialisation in packages/app/src/context/serializeBundle.ts: serializeBundle(bundle) => string producing the agent-agnostic markdown blob (one section per doc, one-line provenance header '### [scope type] Title  ·  id  ·  stale?', sections split by '---', missing target rendered as '### ⚠️ requires_load target missing: <id>' with no body, leading '<!-- loom:context-bundle ... -->' comment). Add bundleVisibilityLines(bundle) => string[] returning the '📄 {Title} — loaded for context' lines walked from the same ordered docs[]. Unit-test both against a known bundle.

Added `packages/app/src/context/serializeBundle.ts` with `serializeBundle(bundle) => string` (leading `<!-- loom:context-bundle target=… mode=… docs=N tokens~=T -->` comment, one `### [scope type] Title · id: … · ⚠️ stale: …` header per doc + body, sections split by `---`, missing target rendered as a bodyless `### ⚠️ requires_load target missing: <id>`) and `bundleVisibilityLines(bundle) => string[]` (`📄 {Title} — loaded for context`, with `(⚠️ stale)` suffix and a `⚠️ requires_load target missing:` line for placeholders) — walked from the same ordered `docs[]` so prompt and visibility can't diverge. Both exported from app index. Covered by the same `tests/context-assembler.test.ts` (serialiser + visibility assertions, all green).

## Step 4 — Add the MCP resource loom://context/{docId}?mode={mode} in packages/mcp/src/resources/context.ts: parse docId + mode from the URI, getState(deps) (the one impure boundary), pass empty overrides (Phase 1), run assembleContext, serialise via serializeBundle, return the text. Register the resource in server.ts. Add an MCP integration test (packages/mcp/tests) that spawns 'loom mcp', reads loom://context for a known doc at mode=chat, and asserts the serialised markdown contains the expected provenance headers.

Added `packages/mcp/src/resources/context.ts` — `handleContextResource(root, uri)`. Builds LoomState via `getState({getActiveLoomRoot, loadWeave, buildLinkIndex, registry, fs, workspaceRoot: root})` (the one impure boundary, mirroring state.ts), runs the pure `assembleContext`, serialises, returns text. Supports two addressing forms: `loom://context/{docId}` and `loom://context/thread/{weaveId}/{threadId}` (anchors on thread.design ?? idea ?? active plan ?? first doc). Registered in `server.ts` (RESOURCE_TEMPLATES + dispatch branch). Integration test added to `packages/mcp/tests/integration.test.ts` ("read loom://context returns serialised bundle with provenance headers") — green.

## Step 5 — Wire the extension's chat-reply launch path to the pipeline: before launching the agent, read loom://context/{chatId}?mode=chat, prepend the serialised bundle to the prompt, and print the bundleVisibilityLines ('📄 … — loaded for context') to the terminal log. This is the path that fixes the demo bug. Verify on a real chat reply that the lines appear and the agent answers from context without grepping.

Wired `packages/vscode/src/commands/chatReply.ts` (launchClaude branch — the demo-bug path). Before launching, reads `loom://context/${chatId}?mode=chat` via the MCP client, prepends the serialised bundle to the prompt with a "pre-loaded — do NOT grep" header, and tells the agent the chat doc is already included. Visibility: derives `📄 … — loaded for context` lines from the SAME returned bundle text (parses provenance headers) and writes them to a "Loom Context" output channel. Best-effort try/catch — if the pipeline is unavailable, logs and proceeds. NOT injected into the sampling (no-Claude) branch — that path builds messages server-side; deferred. Manual VS Code UI exercise NOT performed (no interactive VS Code in this environment); the resource it calls is verified by the integration + real-repo smoke tests.

## Step 6 — Repoint the do-next-step prompt (packages/mcp/src/prompts/doNextStep.ts) to use loom://context/{planId}?mode=implementing instead of its ad-hoc bundling. Confirm the do-step brief still contains idea/design/active-plan + requires_load (now via the assembler).

Repointed `packages/mcp/src/prompts/doNextStep.ts` to `handleContextResource(root, \`loom://context/${planId}?mode=implementing\`)` and removed the now-redundant separate plan-content push + weaveId/threadId path parsing (the plan is the bundle target). The brief still carries idea/design/active-plan + requires_load — now via the assembler.

## Step 7 — Delete the legacy bundler with zero residue: remove packages/mcp/src/resources/threadContext.ts and unregister loom://thread-context in server.ts; migrate any remaining callers to loom://context; update BOTH CLAUDE.md surfaces (repo-root CLAUDE.md and the LOOM_CLAUDE_MD template in packages/app/src/installWorkspace.ts) so the Primary entry points table and the chat-reply context-injection rules name loom://context, not loom://thread-context. Grep the whole repo to confirm no 'thread-context' reference survives.

Deleted `packages/mcp/src/resources/threadContext.ts`; removed its import, RESOURCE_TEMPLATES entry, and dispatch branch from `server.ts`. SCOPE DISCOVERY: the design's §7 checklist only named do-next-step, but **seven** live callers used handleThreadContextResource. Migrated all to loom://context: prompts weaveDesign (mode=design), weavePlan (mode=plan), continueThread (mode=chat) → thread form; tools doStep → doc form `loom://context/${planId}?mode=implementing`; generate.ts (3 sites: design/plan/reference) → thread form (mode left default — inert in P1, Phase 2 wires per-command modes); refreshCtx → thread form (mode=ctx, only when threadId present). Updated prompt strings in extension.ts (refresh-ctx) and summarise.ts. Updated BOTH CLAUDE.md surfaces (repo-root CLAUDE.md + LOOM_CLAUDE_MD template in installWorkspace.ts): Primary-entry-points table and chat-reply context-injection rule now use loom://context/{chat-id}?mode=chat. Updated README.md + packages/vscode/README.md. Grep confirms zero live `handleThreadContextResource`/`loom://thread-context` references in packages/ (only benign: explanatory comments and a local var named `threadContext` in doStep.ts that now holds the bundle text). Historical loom/ docs + CHANGELOG left as-is (conversation history).

## Step 8 — Verify end-to-end: run ./scripts/build-all.sh then ./scripts/test-all.sh, plus the MCP integration test; manually exercise a chat reply and a do-step in the extension and confirm the 📄 visibility lines match the injected docs and the AI answers with context. Record results in the done doc.

Verification: `./scripts/build-all.sh` green (all 6 packages compile after migration + deletion). `./scripts/test-all.sh` green — full suite including the new `tests/context-assembler.test.ts` (11 assertions) and the MCP integration test (8 passed, 0 failed, incl. the new loom://context assertion). Real-repo smoke test (one-off ts-node calling handleContextResource for chat ch_01KRQT5P4KDZQN1PHTEDPSATVV): assembled a 75,744-char bundle containing the global loom/ctx.md and — via its requires_load:[vision,workflow] resolved by SLUG against the live link index — the actual Vision and Workflow reference docs. This validates the demo-bug fix end-to-end (chat reply now gets ctx + parent chain + refs baked in). Manual VS Code extension UI exercise (clicking Chat Reply / DoStep buttons) NOT performed — no interactive VS Code in this environment. Findings for follow-up: (1) getState/loadThread do not load thread/weave ctx from `{thread}/ctx/` subdirs, so thread/weave ctx auto-load is wired-but-inert until getState is extended (owned by ctx-naming/global-ctx threads; extending it ripples into status-derivation and the save switch, so it needs its own design pass) — no regression today (zero ctx/ docs exist); (2) loom/refs/*.md classify as scope 'weave' because getState loads loom/refs/ as a pseudo-weave (cosmetic; refine in Phase 2 reference-load-context); (3) plan-id tool inconsistency: loom_start_plan/complete_step/append_done resolve by ULID (not the filename slug), and the done file is named by ULID.
