---
type: done
id: pl_01KX6WP3PCBVT9X2BCM40M5JPN-done
title: Done — CLI ⇄ MCP ⇄ Extension status-verb parity
status: done
created: 2026-07-10
version: 8
tags: []
parent_id: pl_01KX6WP3PCBVT9X2BCM40M5JPN
requires_load: []
---
# Done — CLI ⇄ MCP ⇄ Extension status-verb parity

## Step 1 — Add a guarded set-status use-case + MCP tool loom_set_status(doc_ulid, status): performs free label transitions (idea/design/reference draft↔active↔done; plan draft↔active) and refuses guarded ones (plan→implementing/done, req→locked) with an error naming the right tool.

Added the guarded single status verb.
- `packages/core/src/setStatus.ts` — pure `decideSetStatus(docType, targetStatus)` → `allow` | `delegate{tool,reason}` | `reject{reason}`. Free label transitions for idea/design/ctx/reference (and plan draft↔active) are allowed; the three guarded transitions delegate (plan→implementing ⇒ loom_start_plan, plan→done ⇒ loom_close_plan, req→locked ⇒ loom_finalize_req); statuses invalid for a type are rejected. Exported from core index.
- `packages/app/src/setStatus.ts` — `setStatus({docUlid,status}, deps)`: resolves the doc, calls the core decision, throws on delegate/reject, else flips status with NO version/`updated` bump (lifecycle, not spec — staleness-safe), idempotent no-op when already at target. Exported from app index.
- `packages/mcp/src/tools/setStatus.ts` — `loom_set_status(doc_ulid, status)`, ULID-strict, resolve-on-miss at the boundary. Registered in server.ts `doc` group.

## Step 2 — Remove the status parameter from loom_update_doc (app use-case + MCP tool) so it only edits body + requires_load; grep-audit every caller that set status via update_doc and migrate it to loom_set_status.

Trimmed status out of `loom_update_doc` so it owns body + requires_load only.
- `packages/mcp/src/tools/updateDoc.ts` — removed the `status` schema property, the `newStatus` read, the status spread, and updated the description + the "at least one of" guard (now content|requires_load, with a pointer to loom_set_status).
- Caller audit: grep confirmed the only in-repo caller passing `status` to loom_update_doc is the extension `markStatus.ts` (migrated in step 6); no app/mcp/cli caller relied on it. This closes the latent bug where update_doc({status:'done'}) could mark a plan done bypassing loom_close_plan.

## Step 3 — Delete the loom_finalize_doc MCP tool and its finalize app use-case (redundant with set_status). Keep loom_finalize_req — that is scope-lock, not status labeling.

Retired the redundant finalize path.
- Deleted `packages/mcp/src/tools/finalizeDoc.ts` and `packages/app/src/finalize.ts`; removed the app-index `finalize` export and the server.ts import + `doc`-group registration of finalizeDoc.
- `loom_finalize_req` deliberately kept — it's scope-lock, not status labeling. The extension's finalize.ts/menu still reference loom_finalize_doc and are removed in step 6 (they compile now since it's only a callTool string).

## Step 4 — Add CLI 'loom set-status <slug> <status>' (resolves slug→ULID at the edge, maps to the set-status use-case). Remove CLI 'finalize'. Purge stale help text — no 'generate permanent ID', slug-addressed not ID.

CLI parity: set-status in, finalize out.
- `packages/cli/src/commands/setStatus.ts` — `loom set-status <doc> <status>`; resolves the human ref (slug/stem/ULID) → canonical id via resolveDocIdOrThrow at the CLI edge, then the guarded use-case.
- Deleted `packages/cli/src/commands/finalize.ts`; in `index.ts` replaced the `finalize <draft>` command (stale "generate its permanent ID" help) with the `set-status` command + import.
- Verified via built CLI: plan→done refused (points to loom_close_plan), invalid status rejected with the valid list, allow+idempotent no-op writes nothing, `loom finalize` now 'unknown command'. build-all green across all packages.

## Step 5 — Unify the CLI create surface: introduce a `loom create <type>` namespace mirroring loom_create_* for ALL types (idea, design, plan, chat, reference, req, weave, thread), and RETIRE `loom weave`. Create commands must NEVER implicitly mint a thread — idea/design/plan/req/chat require an existing thread (resolve slug→ULID, error if missing); `loom create thread` is the explicit thread creator. Also fix the wrong MCP loom_create_idea description ("optionally in a specific thread" — an idea is created only in a specific thread, one per thread) and audit sibling create descriptions.

Unified the CLI create surface to a `loom create <type>` namespace mirroring `loom_create_*`, thread-first (no implicit thread creation), and retired `loom weave`.\n\n- **`packages/cli/src/commands/create.ts`** (new) — 8 handlers: `thread, idea, design, plan, req, chat, reference, weave`. A private `requireThreadUlid(weave, threadRef)` resolves an EXISTING thread (slug or th_ ULID) via `resolveThreadUlid`; on miss it throws `Thread '…' not found. Create it first: loom create thread …` — never auto-creates. `create thread` is the sole explicit creator; `create chat` supports `--refs` for a refs chat; each handler reuses the existing app use-case (weaveIdea/weaveDesign/weavePlan/createThread/chatNew/createReq/createWeave/createReference).\n- **`packages/cli/src/index.ts`** — replaced the `loom weave` group with the `loom create` group (positional `<weave> <thread>` mirrors the MCP thread-first contract; human-first args).\n- **Retired** `packages/cli/src/commands/{weaveIdea,weaveDesign,weavePlan}.ts` and `packages/cli/src/threadArg.ts` (its `ensureThreadUlid` was the auto-create seam — gone).\n- **`packages/app/src/createReference.ts`** (new) — extracted the reference-writing logic out of the MCP tool so CLI + MCP share one implementation (byte-identical files); exported from app index. `packages/mcp/src/tools/createReference.ts` now delegates to it.\n- **`packages/mcp/src/tools/createIdea.ts`** — fixed the wrong description (\"in a weave (optionally in a specific thread)\" → \"in a specific thread — one idea per thread; requires th_ ULID; never mints a thread\") and tightened `thread_ulid` to required in the handler. Audited siblings: create_design/plan/req already read thread-first; no other divergence.\n\nVerified on the built CLI: `loom create -h` lists all 8; `loom weave` is gone; end-to-end `create weave → create thread (mints th_ ULID) → create idea` writes idea.md + thread.md; `create idea` into a missing thread errors with the create-thread hint (no mutation); `create reference` writes via the shared use-case. All scratch artifacts removed, git clean. build-all green.

## Step 6 — Point markStatus.ts at loom_set_status; rename commands loom.markDone/markActive → loom.setStatusDone/setStatusActive with menu labels 'Set Status: Done' / 'Set Status: Active'; remove the loom.finalize command, finalize.ts, and its menu entry.

Repointed the extension's status menu at the guarded verb and removed the dead finalize command.\n- `markStatus.ts` — now calls `loom_set_status({ doc_ulid, status })` (was `loom_update_doc({status})`); functions renamed `markDone/markActiveCommand` → `setStatusDone/setStatusActiveCommand`. The guard's refusal message (e.g. plan→done ⇒ use Close Plan) surfaces via the existing error toast.\n- `extension.ts` — imports + `registerCommand` ids renamed to `loom.setStatusDone` / `loom.setStatusActive`; removed the `loom.finalize` registration and the `finalizeCommand` import.\n- `package.json` — command contributions renamed to `loom.setStatusDone` / `loom.setStatusActive` with titles "Set Status: Done" / "Set Status: Active"; menu `when`-clauses updated to the new ids. (No `loom.finalize` manifest entry existed — it was only registered in code.)\n- Deleted `packages/vscode/src/commands/finalize.ts` (called the retired `loom_finalize_doc`). `loom.finalizeReq` untouched — separate scope-lock command.\n- build-all green (extension compiles, manifest valid). Note for the step-11 sweep: the Set Status menu `when`-clause still lists plan/chat/reference where a 'done'/'active' set may be guarded/invalid — the guard errors clearly, but the menu visibility could be narrowed. The extension needs a Reload Window to pick up the rebuilt bundle for live use.

## Step 7 — Walk the four ways in docs/WAYS-TO-USE-LOOM.md and verify each has the CLI/MCP commands it needs to run end-to-end; record any remaining gaps as follow-ups (esp. Power terminal CLI completeness).

Audited each way in docs/WAYS-TO-USE-LOOM.md against `loom --help`:\n- **① Guided** — extension only, no CLI needed. ✓\n- **② Power terminal** — orientation (`status`, `roadmap`, `next`, `stale`, `blocked`, `search`, `context`, `catalog`, `resources`), full `create <type>`, `set-status`, `start-plan`, `complete-step`, `rename` all present. Doc-management ops covered by the extension. ✓\n- **③ Pure agent (no extension)** — same orientation/create/status coverage, BUT the terminal-only user has **no CLI twin** for the human doc-management ops the extension exposes as tree buttons: `delete`, `archive`, `restore`, `rename-thread`, `move-thread`, `set-priority`, `set-thread-deps`. This is a real coverage gap for the extension-free way. (Agent-driven tools — append-to-chat, do-step, append-done, patch-doc, update-doc — correctly need no CLI twin per the parity rule's single-audience clause.)\n- **④ Automation/CI** — `validate`, `record-release`, `roadmap` present. ✓\n\nNo change to WAYS-TO-USE-LOOM.md needed — it makes no false CLI claims (③'s example uses only `status`/`roadmap`). **Follow-up recorded for the step-11 sweep:** add the missing human doc-management CLI twins (delete/archive/restore/rename-thread/move-thread/set-priority/set-thread-deps) so way ③ is fully terminal-runnable — to be decided (implement now vs. dedicated follow-up thread) during the final sweep.

## Step 8 — Add the 'Tri-surface command parity (hard)' rule to CLAUDE.md (sibling of API-naming / API-refactor-scope), including the WAYS-TO-USE availability clause. Repo-specific — no rule: marker, not mirrored into the LOOM_CLAUDE_MD template.

Added the **Tri-surface command parity (hard)** rule to `CLAUDE.md`, immediately after the API-refactor-scope rule (its natural sibling). It states: a capability is exposed on every surface its consumer needs with names mirrored across CLI ⇄ MCP ⇄ extension; touching one surface obliges mirroring on the others in the same change; exceptions are by-consumer (form differences: CLI human-first vs MCP ULID-strict; single-audience tools stay single-surface — agent-only workflow tools and setup); plus the availability clause tying it to docs/WAYS-TO-USE-LOOM.md (the extension-free Pure-agent way needs CLI twins for the human doc-management ops), and an explicit "verbs must match" clause (the `weave`-vs-`create` split was a parity bug). Repo-specific — carries NO `rule:` marker, so it is not mirrored into the LOOM_CLAUDE_MD template and the claude-md-sync test (marker-set parity) is unaffected, consistent with the sibling API rules.

## Step 9 — Update every surface that names the old commands: README command tables (Mark Done / Finalize → Set Status), WAYS-TO-USE-LOOM.md, docs/CLI_USER_GUIDE.md, loom/refs/mcp-reference.md, loom/refs/api-naming-reference.md; document loom set-status and create-chat.

Swept every LIVE surface that named the old commands; left historical loom/** docs (chats/done/old plans/archived) and CHANGELOG untouched as records.\n- **README.md** — getting-started example now `loom create thread`→`create idea`; added a `loom_set_status` row to the tools table; `loom_update_doc` reworded to "body / requires_load".\n- **packages/cli/README.md** — command table replaced with the 8 `loom create <type>` rows + `loom set-status` (dropped weave/finalize).\n- **packages/vscode/README.md** — "Mark Done / Mark Active" → "Set Status: Done / Active".\n- **docs/CLI_USER_GUIDE.md** — inline `loom weave …` → `loom create …`; command table replaced with create + set-status.\n- **docs/WAYS-TO-USE-LOOM.md** — the Power-terminal chat-loop example now uses the shipped `loom create chat <weave> <thread>` (was "on the roadmap").\n- **loom/refs** (excluded from the gate — normal edits): `cli-commands-reference.md` (create namespace + set-status, dropped weave/finalize), `mcp-reference.md` (doc-group row: finalize→set status, added `loom_set_status`), `vscode-commands-reference.md` (`loom.setStatusDone/Active`), `architecture-reference.md` (use-case list finalize→setStatus), `staleness-reference.md` (finalize→set-status wording).\n- `api-naming-reference.md` needed no change (convention, not command listings). Verified: no stale `loom weave` / `loom finalize` / Mark Done|Active / `loom_finalize_doc` references remain in any live surface.

## Step 10 — Add/adjust tests: set_status free-vs-guarded behavior (plan→done refused, points to close_plan), update_doc no longer accepts status, finalize gone. Run build-all then test-all green.

Tests updated + added; full suite green.\n- **`tests/set-status.test.ts`** (new) — pure `decideSetStatus` guard: free labels (idea/design/plan/reference) allowed; plan→done ⇒ delegate `loom_close_plan`, plan→implementing ⇒ `loom_start_plan`, req→locked ⇒ `loom_finalize_req`; invalid status + unknown doc type rejected. Wired into `scripts/test-all.sh`.\n- **`tests/version-on-content.test.ts`** — migrated: now asserts `loom_update_doc` **rejects** a status-only call (status moved out), and that the app `setStatus` use-case flips status **without** bumping version/updated (the no-false-staleness invariant, at its new home).\n- **`tests/id-management.test.ts`** — migrated off the retired commands: `loom weave idea` → `loom create thread` + `loom create idea`; `loom finalize` → `loom set-status active`. Still asserts ULID minted at create + preserved across the status flip and rename.\n- Audited the rest of the suite: `weaveIdea`/`weaveDesign`/`weavePlan` app use-cases are still imported by several tests and still exist (only the CLI wrappers were retired); `finalizeReq`/close-plan "finalize" are unrelated. No other test referenced the removed symbols.\n- `build-all` green; `test-all` green — all suites pass including the MCP integration subprocess (23/23).

## Step 11 — Final tri-surface discrepancy sweep: walk CLI vs MCP vs extension command-by-command and resolve any remaining name/coverage divergence (verb mismatches, missing twins, stale descriptions), enforcing the parity contract end-to-end.

Walked CLI vs MCP vs extension command-by-command. The two surfaces this plan targeted are now aligned: **status** (one guarded `loom_set_status` mirrored CLI/MCP/extension; `finalize` retired everywhere) and **create** (`loom create <type>` ⇄ `loom_create_*`, thread-first, `loom weave` retired).\n\nOne coherent divergence remains, too large to fold in here without ballooning the plan: a cluster of **human tree-management ops** that have an MCP tool + extension button but **no CLI twin** — `archive`, `delete`, `restore`, `rename-thread`, `move-thread`, `rename-weave`, `set-priority`, `set-thread-deps`, `promote`, `close-plan`, `quick-ship`. This breaks the extension-free **③ Pure agent** way (the parity contract's availability clause). Plus two naming mismatches (`rename`↔`retitle`; extension `weave*` AI-launch buttons vs the new `create` verb).\n\n**Resolution (per the parity contract's "considered gap" clause):** recorded as a dedicated follow-up thread + idea — `cli/cli-management-command-parity` (idea `id_01KX726T6ETMPSAK6TCZA35M06`) — with the full discrepancy table, naming cleanups, success criteria, and non-goals (agent-only tools correctly stay single-surface). Tracked, not silently dropped. Plan auto-completed to done on this step.
